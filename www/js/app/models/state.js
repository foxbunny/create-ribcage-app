// Generated by CoffeeScript 1.6.3
define(function(require) {
  var State, conf, ribcage, state;
  ribcage = require('ribcage');
  conf = require('app/conf');
  State = ribcage.models.LocalStorageModel.extend({
    storageKey: 'state',
    "default": {
      started: false
    }
  });
  state = new State({
    id: conf.name
  });
  state.save(null, {
    forceCreate: true
  });
  state.on('change', function() {
    return state.save();
  });
  return state;
});
