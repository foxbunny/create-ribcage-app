// Generated by CoffeeScript 1.6.3
define(function(require) {
  var App, StatefulRouter, app, conf, hello;
  StatefulRouter = require('ribcage/routers/stateful').Router;
  conf = require('app/conf');
  hello = require('app/routes/hello');
  App = StatefulRouter.extend({
    init: function() {
      this.conf = conf;
      return this.content = '#content';
    },
    routes: {
      '': hello.world
    }
  });
  app = new App();
  return app.start();
});
